import { StyleSheet } from 'react-native';

const formStyles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    paddingTop: 20,
    paddingBottom: 20,
    paddingLeft: 20,
    paddingRight: 20,
  }
})

export { formStyles };
